统一认证授权系统
├─ 基础设施模块
│  ├─ JWT 签发与校验 — 签发 Access/Refresh，校验签名、过期及 iss/aud/jti 等声明
│  ├─ Token 黑名单管理（含过期清理）— 登出/踢线拉黑 jti；后台线程定期清理过期条目
│  ├─ 动态 CORS 配置 — 配置允许源 + 已激活应用 callback 的 Origin，带短时缓存
│  ├─ 接口限流保护 — 按 IP 对部分 API 限频，降低撞库与刷接口风险
│  ├─ 登录失败锁定 — 失败次数达阈值后临时锁定账户（策略可配置）
│  ├─ 滥用检测与安全审计 — 如多账号同 IP 被锁等，写入 security_alert 类审计
│  └─ 授权码存储与消费 — 短期授权码生成、校验、与 redirect_uri 绑定并一次性兑换
├─ Web 门户模块
│  ├─ 首页重定向 — / 跳转至 /login
│  ├─ 登录注册页面 — 登录/注册 UI；可按「注册开关」隐藏注册入口
│  ├─ 管理控制台 — /dashboard，侧栏工作台、概览、系统管理各子页
│  ├─ 静态资源服务 — /static 提供样式与脚本
│  └─ 健康检查 — /health 探活并返回版本号
├─ 认证与令牌模块（/api/v1/auth）
│  ├─ 密码登录认证 — OAuth2 Password；可携带 OAuth 跳转参数衔接授权码流程
│  ├─ 刷新令牌 — 校验 Refresh 与会话表后签发新 Access
│  ├─ 用户登出 — 撤销会话、拉黑 Access、清除 SSO Cookie
│  ├─ 清除 SSO 状态 — 无需登录清除 Cookie，支持安全 next 重定向
│  ├─ 授权码模式（浏览器跳转）— GET /authorize：已登录则回跳带 code，否则进登录页
│  ├─ 生成授权码（JSON）— GET /create-authorization-code：已登录用户取 redirect_url
│  ├─ 令牌获取 — POST /token：authorization_code 或 refresh_token 换 Token
│  ├─ 令牌自省 — POST /introspect：应用凭 client 凭证查询 token 是否有效
│  ├─ 我的会话列表 — GET /sessions/me：展示各会话 IP/UA/过期等
│  ├─ 按 jti 撤销会话 — DELETE /sessions/me：单设备下线并拉黑对应 Access
│  └─ 撤销其他设备会话 — POST /sessions/me/revoke-others：保留当前会话，其余撤销
├─ 用户管理模块（/api/v1/users）
│  ├─ 用户注册 — 受 ALLOW_REGISTRATION / 注册审核策略控制是否可登录
│  ├─ 管理员创建用户 — POST / 后台开户
│  ├─ 当前用户信息 — GET /me
│  ├─ 修改密码 — POST /me/change-password
│  ├─ 用户列表查询 — GET / 分页列表
│  ├─ 用户详情与编辑 — GET/PUT /{user_id}
│  ├─ 禁用用户 — POST /{user_id}/disable，支持时长或永久
│  ├─ 启用用户 — POST /{user_id}/enable
│  ├─ 删除用户 — DELETE /{user_id}
│  └─ 批量删除用户 — POST /batch-delete
├─ 应用管理模块（/api/v1/apps）
│  ├─ 应用注册 — POST /register，返回 app_id 与一次性 app_secret
│  ├─ 批量注册应用 — POST /batch-register
│  ├─ 应用列表与详情 — GET /、GET /list、GET /{app_id}
│  ├─ 工作台应用数据 — GET /workbench 等，供控制台入口展示
│  ├─ 应用信息更新 — PUT /{app_id}
│  ├─ 应用审核启用 — POST /{app_id}/approve；可自动创建资源与 app:{id}:access 权限
│  ├─ 应用禁用 / 启用 — POST /{app_id}/disable、/enable
│  ├─ 批量审核 — POST /batch-approve
│  ├─ 批量禁用 — POST /batch-disable
│  ├─ 应用删除 — DELETE /{app_id} 与 POST /delete 两种调用方式
│  ├─ 批量删除应用 — POST /batch-delete
│  ├─ 应用导出 — GET /export
│  ├─ 批量更新回调地址 — PUT /batch-callbacks；失效动态 CORS 缓存
│  └─ 统一权限校验 — POST /check-permission，业务系统带用户令牌做授权决策
├─ 角色权限模块（/api/v1/rbac）
│  ├─ 我的权限查询 — GET /me/permissions，前端控菜单/按钮显隐
│  ├─ 角色管理 — 角色 CRUD + POST /roles/batch-delete
│  ├─ 角色权限分配 — GET/POST /roles/{id}/permissions
│  ├─ 权限管理 — 权限 CRUD + POST /permissions/batch-delete
│  ├─ 资源管理 — 资源 CRUD + POST /resources/batch-delete
│  ├─ 用户角色分配 — POST /users/{user_id}/roles
│  └─ 用户权限展开查询 — GET /users/{user_id}/permissions
├─ 审计日志模块（/api/v1/logs）
│  ├─ 日志分页查询 — GET /
│  ├─ 日志统计 — GET /stats
│  ├─ 登录趋势 — GET /login-trend
│  ├─ 动作分布 — GET /action-distribution
│  ├─ 安全告警 — GET /security-alerts
│  └─ 日志导出 — GET /export
└─ 系统设置模块（/api/v1/system）
   ├─ 运行时配置 — GET/PUT /settings（如开放注册、注册审核；进程内生效）
   ├─ 注册开关查询 — GET /registration-allowed，公开接口供登录页使用
   └─ 安全概览 — GET /security-overview：黑名单、锁定账户、待审用户、近期告警等